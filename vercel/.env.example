API_KEY=change-me
DOUYIN_COOKIE=
UPSTREAM_TIMEOUT_MS=10000
# Optional static upstream proxy.
# Node runtimes can use a direct HTTP(S) proxy such as http://127.0.0.1:7890
# or a reverse-proxy template such as https://proxy.example.com/fetch?url={url}
UPSTREAM_PROXY_URL=
