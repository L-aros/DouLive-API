API_KEY=change-me
DOUYIN_COOKIE=
UPSTREAM_TIMEOUT_MS=10000
# Workers cannot dial a raw HTTP proxy like 127.0.0.1:7890.
# Use a reverse-proxy template or prefix, for example:
# UPSTREAM_PROXY_URL=https://proxy.example.com/fetch?url={url}
UPSTREAM_PROXY_URL=
